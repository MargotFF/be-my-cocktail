<%= simple_form_for(@cocktail) do |f| %>
  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.submit "Create the cocktail", class: "btn btn-warning" %>
<% end %>

<%= link_to "Cancel", root_path, class: "btn btn-primary" %>
