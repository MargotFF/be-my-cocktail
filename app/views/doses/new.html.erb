<%= simple_form_for [ @cocktail, @dose ] do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.submit "Add the ingredient", class: "btn btn-warning" %>
<% end %>

<%= link_to "Cancel", cocktail_path(@cocktail), class: "btn btn-primary" %>

